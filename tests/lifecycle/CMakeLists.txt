cmake_minimum_required(VERSION 3.16)
project(PlayerbotLifecycleMathTests LANGUAGES CXX)
enable_testing()
add_executable(lifecycle_math
    main.cpp
    ../../playerbot/RandomBotLifecycleMath.cpp
    ../../playerbot/strategy/tests/RandomBotLifecycleMathTest.cpp)
target_compile_features(lifecycle_math PRIVATE cxx_std_14)
target_compile_definitions(lifecycle_math PRIVATE PLAYERBOT_LIFECYCLE_STANDALONE_TEST)
target_include_directories(lifecycle_math PRIVATE ../..)
if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(lifecycle_math PRIVATE -Wall -Wextra -Werror)
endif()
add_test(NAME lifecycle_math COMMAND lifecycle_math)

set(CMANGOS_CORE_SOURCE_DIR "" CACHE PATH "Pinned core checkout with proposed transaction patch applied")
if(CMANGOS_CORE_SOURCE_DIR)
    add_executable(transaction_ack transaction_ack.cpp)
    target_compile_features(transaction_ack PRIVATE cxx_std_14)
    target_include_directories(transaction_ack PRIVATE "${CMANGOS_CORE_SOURCE_DIR}/src/shared")
    # Assertions are test expectations, including in a Release test build.
    if(MSVC)
        target_compile_options(transaction_ack PRIVATE /UNDEBUG)
    else()
        target_compile_options(transaction_ack PRIVATE -UNDEBUG -Wall -Wextra -Werror)
    endif()
    add_test(NAME transaction_ack COMMAND transaction_ack)
    add_executable(estate_intake estate_intake.cpp ../../playerbot/RandomBotEstate.cpp ../../playerbot/RandomBotEstateStore.cpp)
    target_compile_features(estate_intake PRIVATE cxx_std_14)
    target_include_directories(estate_intake PRIVATE stubs ../../playerbot "${CMANGOS_CORE_SOURCE_DIR}/src/shared")
    if(MSVC)
        target_compile_options(estate_intake PRIVATE /UNDEBUG)
    else()
        target_compile_options(estate_intake PRIVATE -UNDEBUG -Wall -Wextra -Werror)
    endif()
    add_test(NAME estate_intake COMMAND estate_intake)

    add_executable(estate_auction_intake estate_auction_intake.cpp ../../playerbot/RandomBotEstateIntake.cpp)
    target_compile_features(estate_auction_intake PRIVATE cxx_std_14)
    target_include_directories(estate_auction_intake PRIVATE stubs ../../playerbot "${CMANGOS_CORE_SOURCE_DIR}/src/shared")
    if(MSVC)
        target_compile_options(estate_auction_intake PRIVATE /UNDEBUG)
    else()
        target_compile_options(estate_auction_intake PRIVATE -UNDEBUG -Wall -Wextra -Werror)
    endif()
    add_test(NAME estate_auction_intake COMMAND estate_auction_intake)
    add_executable(estate_liquidation estate_liquidation.cpp ../../playerbot/RandomBotEstateLiquidation.cpp)
    target_compile_features(estate_liquidation PRIVATE cxx_std_14)
    target_include_directories(estate_liquidation PRIVATE stubs ../../playerbot "${CMANGOS_CORE_SOURCE_DIR}/src/shared")
    if(MSVC)
        target_compile_options(estate_liquidation PRIVATE /UNDEBUG)
    else()
        target_compile_options(estate_liquidation PRIVATE -UNDEBUG -Wall -Wextra -Werror)
    endif()
    add_test(NAME estate_liquidation COMMAND estate_liquidation)
    add_executable(estate_auction_listing estate_auction_listing.cpp ../../playerbot/RandomBotEstateAuctions.cpp)
    target_compile_features(estate_auction_listing PRIVATE cxx_std_14)
    target_include_directories(estate_auction_listing PRIVATE stubs ../../playerbot "${CMANGOS_CORE_SOURCE_DIR}/src/shared")
    if(MSVC)
        target_compile_options(estate_auction_listing PRIVATE /UNDEBUG)
    else()
        target_compile_options(estate_auction_listing PRIVATE -UNDEBUG -Wall -Wextra -Werror)
    endif()
    add_test(NAME estate_auction_listing COMMAND estate_auction_listing)
    add_executable(estate_auction_bid estate_auction_bid.cpp ../../playerbot/RandomBotEstateAuctionBids.cpp)
    target_compile_features(estate_auction_bid PRIVATE cxx_std_14)
    target_include_directories(estate_auction_bid PRIVATE stubs ../../playerbot "${CMANGOS_CORE_SOURCE_DIR}/src/shared")
    if(MSVC)
        target_compile_options(estate_auction_bid PRIVATE /UNDEBUG)
    else()
        target_compile_options(estate_auction_bid PRIVATE -UNDEBUG -Wall -Wextra -Werror)
    endif()
    add_test(NAME estate_auction_bid COMMAND estate_auction_bid)
    add_executable(estate_auction_settlement estate_auction_settlement.cpp ../../playerbot/RandomBotEstateAuctionSettlement.cpp)
    target_compile_features(estate_auction_settlement PRIVATE cxx_std_14)
    target_include_directories(estate_auction_settlement PRIVATE stubs ../../playerbot "${CMANGOS_CORE_SOURCE_DIR}/src/shared")
    if(MSVC)
        target_compile_options(estate_auction_settlement PRIVATE /UNDEBUG)
    else()
        target_compile_options(estate_auction_settlement PRIVATE -UNDEBUG -Wall -Wextra -Werror)
    endif()
    add_test(NAME estate_auction_settlement COMMAND estate_auction_settlement)
endif()
